name: mv_ou_process/
device: cuda:1
num_runs: 1
seed: 1


args:
  dim: [1024]                # dimension of the process
  T: [.01]              # last time point
  num_steps: [100]      # number of time steps
  num_samples: [1000]    # ensemble size
  batch_size: 500
  path: /raid/data/neep
  diagonal: False      # if matrices A, B are diagonal
  unitary: True
