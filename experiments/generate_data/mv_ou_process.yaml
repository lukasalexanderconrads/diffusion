name: mv_ou_process/
device: cuda:1
num_runs: 1
seed: 1


args:
  dim: [3]                # dimension of the process
  T: [40]              # last time point
  num_steps: [5000]      # number of time steps
  num_samples: [5000]    # ensemble size
  batch_size: 100
  path: /raid/data/neep
  diagonal: False      # if matrices A, B are diagonal
